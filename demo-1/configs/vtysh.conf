! Tắt integrated config để:
!   1. vtysh không cố parse /etc/frr/frr.conf mỗi lần chạy -> hết 2 dòng lỗi nhiễu
!      ("Can't open configuration file /etc/frr/vtysh.conf" + "processing failure: 11")
!      mà học viên dễ tưởng nhầm là lỗi cần tìm.
!   2. Lệnh `write` ghi ra ospfd.conf/zebra.conf TRONG container, không ghi đè
!      frr.conf đang bind-mount từ repo -> lỗi cắm sẵn không bị mất, lab chạy lại được.
no service integrated-vtysh-config
